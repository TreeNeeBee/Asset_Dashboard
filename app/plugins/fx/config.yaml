# ─────────────────────────────────────────────────────────────
# Foreign Exchange Plugin Configuration
# ─────────────────────────────────────────────────────────────

# Plugin metadata
key: "fx"
name: "Foreign Exchange"
category: "fx"
description: "Foreign exchange rates via ExchangeRate-API (USD/CNY, EUR/USD …)"
version: "1.0.0"

# Grafana panel display
panel_title_prefix: "FX"
close_column_label: "Rate"

# Fetch interval in milliseconds (minimum: 1ms)
fetch_interval_ms: 120000

# Data source settings
source:
  name: "ExchangeRate FX"
  provider: "fx_exchange_rate"
  base_url: "https://open.er-api.com/v6"
  description: "Foreign exchange rates via ExchangeRate-API"

# API key file path (empty = no key needed)
api_key_file: ""

# Tracked assets
assets:
  - symbol: "USD/CNY"
    display_name: "US Dollar / Chinese Yuan"
  - symbol: "EUR/USD"
    display_name: "Euro / US Dollar"
  - symbol: "GBP/USD"
    display_name: "British Pound / US Dollar"
  - symbol: "USD/JPY"
    display_name: "US Dollar / Japanese Yen"
